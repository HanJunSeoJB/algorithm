#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int num;
	char flag[20] = { 0, };
	char name[20];
	char* tmp;

	printf("이름 입력: ");
	scanf("%s", name);
	printf("\n학번 입력: ");
	scanf("%d", &num);

	for (int i = 0; name[i]; i++) {
		if ('a' > name[i]) {
			name[i] += 32;
			flag[i] = 1;
		}
	}
	printf("%s", name);

	for (int i = 0; name[i]; i++) {
		for (int j = 0; name[i]; j++) {
			if (name[i] < name[j]) {
				tmp = name[i];
				name[i] = name;
			}
		}
	}*/
	return 0;
}
